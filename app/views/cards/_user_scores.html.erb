<ul>
  <% card.scores.includes(:user).each do |score| %>
    <li id="score_<%= score.id %>">
      <%= score.user.name %>: 
      <%= card.visible? ? score.score : "✅" %>
    </li>
  <% end %>
</ul>